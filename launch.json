{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: FastAPI",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app:app",  // Ensure "app:app" matches your filename and FastAPI instance
                "--reload"
            ],
            "jinja": true,
            "console": "integratedTerminal"
        }
    ]
}
